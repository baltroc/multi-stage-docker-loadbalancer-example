# dac
todo readme


instance principale : 172.28.100.202, load balancer sur le port 8081

ansible-playbook --private-key id_ubuntu.pem -i hosts server.yml


ansible-playbook --private-key id_ubuntu.pem -i hosts caddy.yml
